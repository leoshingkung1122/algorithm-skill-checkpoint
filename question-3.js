/*
What is the Big O notation for Function findStudentById . Explain your reason.
Answer:

O(n) because the function is iterating through the students in the array .


*/

/* 
What is the Big O notation for Function findProductPrice. Explain your reason.
Answer:

O(log n) because the function is using binary search to find the product price.

*/

/* 
Which function is more efficient and why?
Answer:

findProductPrice is more efficient because it uses binary search to find the product price, 
which is O(log n) compared to O(n) for findStudentById.


*/
